const APP_VERSION="2.3.0", DB_NAME="FarhadTrainerV2DB", DB_VERSION=1;
const DAYS=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];
function ex(n,sets,reps,rest,target,primary,secondary,steps,mistakes,sub){return{n,sets,reps,rest,target,primary,secondary,steps,mistakes,sub,yt:`https://www.youtube.com/results?search_query=${encodeURIComponent(n+" proper form")}`}}
const WORKOUTS={
1:{name:"Push + Core",focus:"Upper chest · chest · shoulders · triceps · core",ex:[
ex("Incline Dumbbell Press",4,"8–10",120,"7–8","Upper chest",["Front delts","Triceps"],["Set the bench to a low incline.","Pull shoulder blades down and back.","Lower dumbbells under control to upper-chest level.","Press up without bouncing or shrugging."],["Bench angle too steep","Elbows flared straight out","Shoulders rolling forward"],"Feet-elevated push-ups"),
ex("Flat Dumbbell Press",3,"10–12",90,"8","Chest",["Front delts","Triceps"],["Plant feet firmly.","Keep shoulder blades gently retracted.","Lower dumbbells with forearms near vertical.","Press evenly without clanking dumbbells."],["Bouncing at the bottom","Elbows at 90° to torso","Losing upper-back tension"],"Dumbbell floor press"),
ex("Standing Dumbbell Shoulder Press",3,"8–10",90,"8","Shoulders",["Triceps","Core"],["Stand tall and brace your abs.","Start dumbbells near shoulder height.","Press up without leaning back.","Finish overhead with ribs controlled."],["Overarching lower back","Excessive lean","Short range of motion"],"Seated dumbbell shoulder press"),
ex("Lateral Raise",3,"12–15",60,"8","Side delts",["Upper traps"],["Use light-to-moderate dumbbells.","Lead with elbows.","Raise to about shoulder height.","Lower slowly without swinging."],["Using momentum","Shrugging hard","Turning it into a front raise"],"Band lateral raise"),
ex("Overhead Triceps Extension",3,"12",60,"8","Triceps",["Core"],["Brace ribs down.","Keep upper arms mostly vertical.","Lower the dumbbell behind the head.","Extend elbows without excessive flaring."],["Arching lower back","Elbows drifting wide","Moving shoulders instead of elbows"],"Close-grip push-ups"),
ex("Plank",3,"45–60 sec",45,"7","Core",["Glutes","Shoulders"],["Place elbows under shoulders.","Squeeze glutes gently.","Keep ribs and pelvis stacked.","Breathe while holding a straight line."],["Hips sagging","Hips too high","Holding breath"],"Dead bug")]},
2:{name:"Pull + Core",focus:"Lats · mid-back · rear delts · biceps · core",ex:[
ex("One-arm Dumbbell Row",4,"8–10",90,"8","Lats",["Mid-back","Rear delts","Biceps"],["Support one hand on a bench.","Keep spine neutral.","Pull elbow toward your hip.","Pause briefly then lower under control."],["Rotating the torso","Shrugging the shoulder","Yanking with momentum"],"Chest-supported dumbbell row"),
ex("Dumbbell Pullover",4,"10–12",90,"8","Lats",["Chest","Triceps"],["Lie securely on a bench.","Keep ribs gently down.","Lower behind your head with soft elbows.","Pull back over using your lats."],["Overarching the back","Going beyond comfortable shoulder range","Turning it into a triceps extension"],"Band straight-arm pulldown"),
ex("Rear Delt Fly",3,"12–15",60,"8","Rear delts",["Mid-back"],["Hinge forward with a neutral spine.","Keep elbows softly bent.","Open arms out and slightly back.","Move through shoulders, not the neck."],["Shrugging","Swinging dumbbells","Using too much weight"],"Band reverse fly"),
ex("Dumbbell Curl",3,"10–12",60,"8","Biceps",["Forearms"],["Stand tall with elbows by your sides.","Curl without swinging.","Squeeze at the top.","Lower fully under control."],["Elbows drifting forward","Using hip momentum","Partial lowering"],"Alternating dumbbell curl"),
ex("Hammer Curl",2,"12",60,"8","Biceps",["Brachialis","Forearms"],["Keep palms facing inward.","Keep elbows close.","Curl smoothly.","Control the eccentric."],["Swinging","Shrugging","Moving elbows forward"],"Cross-body hammer curl"),
ex("Side Plank",3,"30–45 sec/side",45,"7","Obliques",["Glutes","Shoulders"],["Stack elbow under shoulder.","Lift hips.","Keep head, ribs, hips and ankles aligned.","Breathe normally."],["Shoulder collapsing","Hips rotating","Hips dropping"],"Suitcase hold")]},
3:{name:"Legs + Shoulders",focus:"Quads · glutes · hamstrings · calves · delts",ex:[
ex("Goblet Squat",4,"10",120,"8","Quads",["Glutes","Core","Adductors"],["Hold one dumbbell close to your chest.","Sit down between your hips.","Keep the whole foot planted.","Drive up while knees track over toes."],["Heels lifting","Knees collapsing inward","Lower back rounding"],"Dumbbell front squat"),
ex("Dumbbell Romanian Deadlift",4,"8–10",120,"8","Hamstrings",["Glutes","Back extensors","Grip"],["Stand tall with dumbbells close to thighs.","Push hips back.","Keep knees softly bent.","Stop when hamstrings are loaded, then drive hips forward."],["Squatting the movement","Dumbbells drifting away","Rounding lower back"],"Single-leg dumbbell RDL"),
ex("Bulgarian Split Squat",3,"10/leg",90,"8","Quads",["Glutes","Hamstrings","Core"],["Place rear foot on a bench.","Set front foot far enough for balance.","Lower mostly straight down.","Drive through the front foot."],["Front foot too close","Falling forward","Pushing mostly from rear leg"],"Reverse dumbbell lunge"),
ex("Standing Calf Raise",3,"15",60,"8","Calves",["Ankle stabilizers"],["Stand tall and use support if needed.","Rise onto the balls of your feet.","Pause at the top.","Lower through a full comfortable range."],["Bouncing","Rolling ankles outward","Short range of motion"],"Single-leg calf raise"),
ex("Lateral Raise",3,"15",60,"8","Side delts",["Upper traps"],["Use light-to-moderate dumbbells.","Lead with elbows.","Raise to shoulder height.","Lower slowly."],["Swinging","Shrugging","Going far above shoulder height"],"Band lateral raise"),
ex("Farmer Carry",3,"40–60 sec",60,"7","Grip",["Core","Traps","Glutes"],["Hold heavy dumbbells at your sides.","Stand tall with ribs stacked.","Walk with short controlled steps.","Keep shoulders down and grip firm."],["Leaning side to side","Shrugging constantly","Rushing with sloppy steps"],"Suitcase carry")]} };

let db,settings={id:"main",version:APP_VERSION,currentWeek:1,measurementInterval:14,weightUnit:"lb",migratedToLb:false},deferredPrompt=null,restTimerId=null,restLeft=90,restPaused=false;
const el=id=>document.getElementById(id), today=()=>new Date().toISOString().slice(0,10), escapeHtml=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
function toast(msg){const t=el("toast");t.textContent=msg;t.classList.remove("hidden");setTimeout(()=>t.classList.add("hidden"),1700)}
function openDB(){return new Promise((res,rej)=>{const r=indexedDB.open(DB_NAME,DB_VERSION);r.onupgradeneeded=()=>{const d=r.result;["settings","drafts","sessions","metrics","checkins"].forEach(s=>{if(!d.objectStoreNames.contains(s))d.createObjectStore(s,{keyPath:"id"})})};r.onsuccess=()=>{db=r.result;res(db)};r.onerror=()=>rej(r.error)})}
function put(s,o){return new Promise((res,rej)=>{const r=db.transaction(s,"readwrite").objectStore(s).put(o);r.onsuccess=()=>res(o);r.onerror=()=>rej(r.error)})}function get(s,id){return new Promise((res,rej)=>{const r=db.transaction(s).objectStore(s).get(id);r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}function all(s){return new Promise((res,rej)=>{const r=db.transaction(s).objectStore(s).getAll();r.onsuccess=()=>res(r.result||[]);r.onerror=()=>rej(r.error)})}function del(s,id){return new Promise((res,rej)=>{const r=db.transaction(s,"readwrite").objectStore(s).delete(id);r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})}function clearStore(s){return new Promise((res,rej)=>{const r=db.transaction(s,"readwrite").objectStore(s).clear();r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})}

const KG_TO_LB=2.2046226218;
function round1(n){return Math.round(n*10)/10}
async function migrateWeightsToLbOnce(){
  const saved=await get("settings","main");
  if(saved)settings={...settings,...saved};
  if(settings.migratedToLb){settings.weightUnit="lb";await saveSettings();return}
  for(const ses of await all("sessions")){let ch=false;ses.exercises?.forEach(row=>row.sets?.forEach(set=>{if(Number(set.weight)>0){set.weight=round1(set.weight*KG_TO_LB);ch=true}}));if(ch)await put("sessions",ses)}
  for(const d of await all("drafts")){let ch=false;d.data?.exercises?.forEach(row=>row.sets?.forEach(set=>{if(Number(set.weight)>0){set.weight=round1(set.weight*KG_TO_LB);ch=true}}));if(ch)await put("drafts",d)}
  for(const m of await all("metrics")){if(Number(m.weight)>0){m.weight=round1(m.weight*KG_TO_LB);await put("metrics",m)}}
  for(const c of await all("checkins")){if(Number(c.weight)>0){c.weight=round1(c.weight*KG_TO_LB);await put("checkins",c)}}
  settings.migratedToLb=true;settings.weightUnit="lb";await saveSettings();
}
function phaseFor(w){w=+w;if(w<=4)return{name:"Adaptation",title:"Adaptation phase",copy:"Build technique and consistency. Use loads you can control for the full rep range with 2–3 good reps still possible."};if(w<=7)return{name:"Progression",title:"Progression phase",copy:"Add reps first. Once every valid set reaches the top of the rep range with clean form, increase load by 2.5–5%."};if(w===8)return{name:"Deload",title:"Deload week",copy:"Reduce total sets by about 30–40%. Keep movement quality high and avoid grinding reps."};return{name:"Peak",title:"Peak phase",copy:"Compounds mostly 6–10 reps and isolation 10–15. Keep hard sets controlled and avoid repeated failure."}}
function range(txt){const n=String(txt).match(/\d+/g)||[];return{low:+n[0]||0,high:+n[n.length-1]||0}}function roundHalf(n){return Math.round(n*2)/2}function weekStart(){const n=new Date(),s=new Date(n);s.setDate(n.getDate()-n.getDay());s.setHours(0,0,0,0);return s}
async function saveSettings(){settings.version=APP_VERSION;await put("settings",settings)}
function fillDays(id,val){const s=el(id);s.innerHTML="";DAYS.forEach((d,i)=>{const o=document.createElement("option");o.value=i;o.textContent=d;s.appendChild(o)});s.value=val}
function initControls(){const w=el("weekSelect");for(let i=1;i<=12;i++){const o=document.createElement("option");o.value=i;o.textContent=`Week ${i}`;w.appendChild(o)}w.value=settings.currentWeek;w.onchange=async()=>{settings.currentWeek=+w.value;await saveSettings();await renderHome()};el("workoutDate").value=today();if(el("measurementInterval")){el("measurementInterval").value=settings.measurementInterval||14;el("measurementInterval").onchange=async()=>{settings.measurementInterval=+el("measurementInterval").value;await saveSettings();await renderProgress();await renderHome()}}}
async function navigate(page){document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));el(page).classList.add("active");document.querySelectorAll(".bottom-nav button").forEach(b=>b.classList.toggle("active",b.dataset.page===page));if(page==="home")await renderHome();if(page==="plan")renderPlan();if(page==="workout")await renderWorkoutPage();if(page==="learn")renderLibrary();if(page==="progress")await renderProgress();if(page==="checkin")await renderCoachSummary()}
document.querySelectorAll(".bottom-nav button").forEach(b=>b.onclick=()=>navigate(b.dataset.page));
async function sessionsThisWeek(){const h=await all("sessions"),s=weekStart(),e=new Date(s);e.setDate(s.getDate()+7);return h.filter(x=>{const d=new Date(x.date+"T12:00:00");return d>=s&&d<e})}
async function latestMetric(){const m=await all("metrics");return m.sort((a,b)=>a.createdAt-b.createdAt).at(-1)}
function exerciseType(e){if(e.n==="Plank"||e.n==="Side Plank")return "timed";if(e.n==="Farmer Carry")return "timed_weighted";return "weighted"}
function validSet(e,s){if(!s?.done)return false;const t=exerciseType(e);if(t==="weighted")return Number(s.weight)>0&&Number(s.reps)>0;if(t==="timed")return Number(s.seconds)>0;if(t==="timed_weighted")return Number(s.weight)>0&&Number(s.seconds)>0;return false}
async function prCount(){const h=await all("sessions"),best={};h.forEach(w=>w.exercises?.forEach(row=>{const e=findEx(row.name);row.sets?.forEach(s=>{if(e&&validSet(e,s)){const sc=exerciseType(e)==="weighted"?s.weight*s.reps:exerciseType(e)==="timed_weighted"?s.weight*s.seconds:s.seconds;if(!best[row.name]||sc>best[row.name])best[row.name]=sc}})}));return Object.keys(best).length}
async function nextWorkoutId(){const h=(await all("sessions")).sort((a,b)=>b.completedAt-a.completedAt);return h.length?(h[0].workoutId%3)+1:1}
async function renderHome(){const h=await all("sessions"),wh=await sessionsThisWeek(),m=await latestMetric();el("homeWeight").textContent=m?.weight??"—";el("homeSessions").textContent=`${wh.length}/3`;el("homePRs").textContent=await prCount();el("homeAdherence").textContent=`${Math.min(100,Math.round(h.length/(Math.max(1,settings.currentWeek)*3)*100))}%`;const nextId=await nextWorkoutId(),w=WORKOUTS[nextId],letter=["","A","B","C"][nextId];el("nextWorkoutTitle").textContent=`Workout ${letter} — ${w.name}`;el("nextWorkoutMeta").textContent=`${w.focus} · ${w.ex.length} exercises · train whenever your week allows`;el("nextWorkoutMuscles").innerHTML=[...new Set(w.ex.flatMap(x=>[x.primary,...x.secondary]))].slice(0,8).map(x=>`<span class="muscle-pill">${x}</span>`).join("");const p=phaseFor(settings.currentWeek);el("phaseChip").textContent=p.name;el("phaseTitle").textContent=p.title;el("phaseCopy").textContent=p.copy;renderSequence(wh,nextId);renderWeeklyMap(wh);await renderMeasurementDue()}
function renderSequence(wh,nextId){el("weekStrip").innerHTML=[1,2,3].map(id=>{const done=wh.some(s=>s.workoutId===id),letter=["","A","B","C"][id];return`<div class="day-tile ${done?"done":id===nextId?"train":""}"><strong>Workout ${letter}</strong>${WORKOUTS[id].name}${done?"<br>Complete":id===nextId?"<br>Next":""}</div>`}).join("")}
function renderWeeklyMap(wh){const m=new Set();wh.forEach(s=>s.exercises?.forEach(e=>{const d=findEx(e.name);if(d){m.add(d.primary);d.secondary.forEach(x=>m.add(x))}}));el("weeklyMuscleMap").innerHTML=muscleMap([...m],[])}
function renderPlan(){el("planCards").innerHTML=[1,2,3].map(id=>{const w=WORKOUTS[id],letter=["","A","B","C"][id];return`<article class="panel"><div class="eyebrow">WORKOUT ${letter}</div><h2>${w.name}</h2><p class="muted">${w.focus}</p>${w.ex.map(e=>`<div class="history-card"><strong>${e.n}</strong><div class="exercise-meta">${e.sets} sets · ${e.reps} · rest ${e.rest}s</div><div class="focus-row"><span>${e.primary}</span>${e.secondary.map(x=>`<span>${x}</span>`).join("")}</div></div>`).join("")}</article>`}).join("")}

function draftId(){return`draft:${el("workoutDate").value}:${el("workoutSelect").value}`}function findEx(name){for(const w of Object.values(WORKOUTS)){const e=w.ex.find(x=>x.n===name);if(e)return e}return null}
async function lastEx(name){const h=(await all("sessions")).sort((a,b)=>b.completedAt-a.completedAt);for(const s of h){const e=s.exercises?.find(x=>x.name===name);if(e)return e}return null}
async function progFor(e){const last=await lastEx(e.n);if(!last)return{previous:"No history",suggested:"Start conservative",coach:"Choose a load you can control through the full target rep range.",numeric:null};const done=last.sets.filter(s=>validSet(e,s));if(!done.length)return{previous:"No valid completed sets",suggested:"Repeat target",coach:"Complete valid sets first so the app can recommend progression.",numeric:null};const t=exerciseType(e),ls=done.at(-1);if(t!=="weighted"){const prev=t==="timed"?`${ls.seconds} sec`:`${ls.weight} lb × ${ls.seconds} sec`;return{previous:prev,suggested:prev,coach:"Keep the same target and improve control or duration within the range.",numeric:null}}const r=range(e.reps),prev=`${ls.weight} lb × ${ls.reps}`,allTop=done.length>=e.sets&&done.every(s=>s.reps>=r.high),failed=done.some(s=>s.reps<r.low);if(failed)return{previous:prev,suggested:`~${roundHalf(ls.weight*.95)} lb`,coach:"A set fell below the rep range. Reduce load about 5% and rebuild clean reps.",numeric:roundHalf(ls.weight*.95)};if(allTop)return{previous:prev,suggested:`${roundHalf(ls.weight*1.025)}–${roundHalf(ls.weight*1.05)} lb`,coach:"All sets reached the top of the rep range. Increase load 2.5–5% next time.",numeric:roundHalf(ls.weight*1.025)};return{previous:prev,suggested:`${ls.weight} lb`,coach:"Keep the same load and add reps until every set reaches the top of the range.",numeric:ls.weight}}
async function renderWorkout(){const id=+el("workoutSelect").value,w=WORKOUTS[id],draft=await get("drafts",draftId()),letter=["","A","B","C"][id];el("workoutTitle").textContent=`Workout ${letter} — ${w.name}`;el("workoutFocus").textContent=w.focus;const holder=el("exerciseCards");holder.innerHTML="";for(let ei=0;ei<w.ex.length;ei++){const e=w.ex[ei],p=await progFor(e),card=document.createElement("article");card.className="panel exercise-card";const type=exerciseType(e);const sets=Array.from({length:e.sets},(_,si)=>{const v=draft?.data?.exercises?.[ei]?.sets?.[si]||{};let inputs;if(type==="timed")inputs=`<input data-e="${ei}" data-s="${si}" data-f="seconds" type="number" step="1" placeholder="sec" value="${v.seconds??""}"><input disabled value="—">`;else if(type==="timed_weighted")inputs=`<input data-e="${ei}" data-s="${si}" data-f="weight" type="number" step=".5" placeholder="lb" value="${v.weight??""}"><input data-e="${ei}" data-s="${si}" data-f="seconds" type="number" step="1" placeholder="sec" value="${v.seconds??""}">`;else inputs=`<input data-e="${ei}" data-s="${si}" data-f="weight" type="number" step=".5" placeholder="lb" value="${v.weight??""}"><input data-e="${ei}" data-s="${si}" data-f="reps" type="number" step="1" placeholder="reps" value="${v.reps??""}">`;return`<div class="set-row" data-setrow="${ei}-${si}"><span>S${si+1}</span>${inputs}<input class="done-check" data-e="${ei}" data-s="${si}" data-f="done" type="checkbox" ${v.done?"checked":""}></div>`}).join("");const rule=type==="weighted"?"Counts only when checked, weight > 0 and reps > 0.":type==="timed"?"Counts only when checked and seconds > 0.":"Counts only when checked, weight > 0 and seconds > 0.";card.innerHTML=`<div class="exercise-top" data-toggle="${ei}"><div><div class="exercise-title">${e.n}</div><div class="exercise-meta">${e.sets} × ${e.reps} · rest ${e.rest}s</div></div><span class="expand-hint">${ei===0?"Open":"Tap"}</span></div><div class="exercise-body ${ei===0?"":"hidden"}" id="exerciseBody${ei}"><div class="muscle-layout"><div class="muscle-map-wrap">${muscleMap([e.primary],e.secondary)}</div><div class="muscle-copy"><div><strong>Primary muscle</strong><span>${e.primary}</span></div><div><strong>Secondary muscles</strong><span>${e.secondary.join(", ")}</span></div><div><strong>Substitution</strong><span>${e.sub}</span></div></div></div><div class="compare-grid"><div class="compare-box"><small>PREVIOUS</small><strong>${p.previous}</strong></div><div class="compare-box"><small>SUGGESTED</small><strong>${p.suggested}</strong></div><div class="compare-box coach-box"><small>COACH</small><strong>${p.coach}</strong></div></div><div class="exercise-links"><a class="link-btn" href="${e.yt}" target="_blank" rel="noopener">YouTube demos ↗</a><button class="link-btn inline-timer-btn" data-rest="${e.rest}">Start ${e.rest}s rest</button>${p.numeric?`<button class="link-btn applySuggestion" data-ei="${ei}" data-weight="${p.numeric}">Apply suggested weight</button>`:""}</div><div class="form-guide"><div class="guide-box"><h4>HOW TO DO IT</h4><ul>${e.steps.map(x=>`<li>${x}</li>`).join("")}</ul></div><div class="guide-box"><h4>COMMON MISTAKES</h4><ul>${e.mistakes.map(x=>`<li>${x}</li>`).join("")}</ul></div></div><div class="set-head"><span>SET</span><span>${type==="timed"?"SEC":"LB"}</span><span>${type==="weighted"?"REPS":"SEC"}</span><span>✓</span></div>${sets}<div class="tiny muted set-rule">${rule}</div></div>`;holder.appendChild(card)}holder.querySelectorAll(".exercise-top").forEach(t=>t.onclick=()=>el("exerciseBody"+t.dataset.toggle).classList.toggle("hidden"));holder.querySelectorAll(".inline-timer-btn").forEach(b=>b.onclick=()=>startRest(+b.dataset.rest));holder.querySelectorAll(".applySuggestion").forEach(b=>b.onclick=()=>{holder.querySelectorAll(`[data-e="${b.dataset.ei}"][data-f="weight"]`).forEach(i=>i.value=b.dataset.weight);saveDraft()});holder.querySelectorAll("input[data-e]").forEach(i=>i.addEventListener("change",saveDraft));el("sessionScore").value=draft?.data?.sessionScore??"";el("sessionNotes").value=draft?.data?.notes??""}
function collectDraft(){const id=+el("workoutSelect").value,w=WORKOUTS[id],exercises=w.ex.map(e=>({name:e.n,sets:Array.from({length:e.sets},()=>({weight:"",reps:"",seconds:"",done:false}))}));document.querySelectorAll("#exerciseCards input[data-e]").forEach(i=>{const e=+i.dataset.e,s=+i.dataset.s,f=i.dataset.f;exercises[e].sets[s][f]=i.type==="checkbox"?i.checked:(i.value===""?"":+i.value)});return{workoutId:id,date:el("workoutDate").value,sessionScore:el("sessionScore").value===""?"":+el("sessionScore").value,notes:el("sessionNotes").value,exercises}}
async function saveDraft(){await put("drafts",{id:draftId(),updatedAt:Date.now(),data:collectDraft()})}el("saveDraftBtn").onclick=async()=>{await saveDraft();toast("Draft saved")};el("sessionScore").onchange=saveDraft;el("sessionNotes").onchange=saveDraft;el("workoutSelect").onchange=renderWorkout;el("workoutDate").onchange=renderWorkout;
el("finishWorkoutBtn").onclick=async()=>{const d=collectDraft();let valid=0,volume=0;d.exercises.forEach(row=>{const e=findEx(row.name);row.sets.forEach(s=>{s.valid=validSet(e,s);if(s.valid){valid++;if(exerciseType(e)==="weighted")volume+=s.weight*s.reps;else if(exerciseType(e)==="timed_weighted")volume+=s.weight*s.seconds}})});if(!valid){toast("No valid completed sets yet");d.exercises.forEach((row,ei)=>{const e=findEx(row.name);row.sets.forEach((s,si)=>{if(s.done&&!validSet(e,s))document.querySelector(`[data-setrow="${ei}-${si}"]`)?.classList.add("invalid-set")})});return}const old=await all("sessions"),best={};old.forEach(w=>w.exercises?.forEach(row=>{const e=findEx(row.name);row.sets?.forEach(s=>{if(e&&validSet(e,s)){const sc=exerciseType(e)==="weighted"?s.weight*s.reps:exerciseType(e)==="timed_weighted"?s.weight*s.seconds:s.seconds;if(!best[row.name]||sc>best[row.name])best[row.name]=sc}})}));let prs=0;d.exercises.forEach(row=>{const e=findEx(row.name);row.sets.forEach(s=>{if(validSet(e,s)){const sc=exerciseType(e)==="weighted"?s.weight*s.reps:exerciseType(e)==="timed_weighted"?s.weight*s.seconds:s.seconds;if(!best[row.name]||sc>best[row.name])prs++}})});await put("sessions",{id:`session:${d.date}:${d.workoutId}:${Date.now()}`,completedAt:Date.now(),date:d.date,workoutId:d.workoutId,name:WORKOUTS[d.workoutId].name,sessionScore:d.sessionScore||null,notes:d.notes,volume:Math.round(volume),prs,exercises:d.exercises});await del("drafts",draftId());toast(prs?`Workout saved · ${prs} PR${prs>1?"s":""}`:"Workout saved");await renderWorkoutPage();await renderHome()};
async function renderHistory(){
  const list=(await all("sessions")).sort((x,y)=>y.completedAt-x.completedAt);
  el("workoutHistoryList").innerHTML=list.length?list.map(s=>`<details class="history-card"><summary>${s.date} · Workout ${["","A","B","C"][s.workoutId]} — ${s.name} · ${s.volume} volume</summary><div class="muted tiny">Score ${s.sessionScore??"—"}/10 · PRs ${s.prs||0}</div>${s.exercises.map(row=>{const e=findEx(row.name),sets=row.sets.filter(x=>validSet(e,x));return`<div class="history-card"><strong>${row.name}</strong>${sets.map((set,i)=>`<div class="history-set"><span>Set ${i+1}</span><span>${set.weight||"—"} lb</span><span>${set.reps||set.seconds||"—"}</span></div>`).join("")||'<div class="muted tiny">No valid completed sets</div>'}</div>`}).join("")}${s.notes?`<div class="notice">${escapeHtml(s.notes)}</div>`:""}<div class="history-actions"><button type="button" class="delete-history" data-delete-session="${s.id}">Delete workout</button></div></details>`).join(""):'<div class="muted">No completed workouts yet.</div>';
  el("workoutHistoryList").querySelectorAll("[data-delete-session]").forEach(b=>b.onclick=()=>deleteHistoryEntry(b.dataset.deleteSession));
}
async function deleteHistoryEntry(id){if(!confirm("Delete this workout history entry? This cannot be undone."))return;await del("sessions",id);toast("Workout deleted");await renderHistory();await renderHome()}
function muscleMap(primary=[],secondary=[]){const P=new Set(primary.flatMap(normMuscle)),S=new Set(secondary.flatMap(normMuscle)),c=z=>P.has(z)?"primary":S.has(z)?"secondary":"base";return`<svg viewBox="0 0 250 145" class="body-svg"><text x="55" y="10">FRONT</text><text x="175" y="10">BACK</text><circle class="base" cx="70" cy="25" r="9"/><rect class="${c("shoulders")}" x="50" y="37" width="40" height="12" rx="6"/><rect class="${c("chest")}" x="55" y="48" width="30" height="19" rx="8"/><rect class="${c("core")}" x="58" y="67" width="24" height="24" rx="8"/><rect class="${c("biceps")}" x="41" y="48" width="9" height="28" rx="4"/><rect class="${c("biceps")}" x="90" y="48" width="9" height="28" rx="4"/><rect class="${c("forearms")}" x="39" y="76" width="8" height="27" rx="4"/><rect class="${c("forearms")}" x="93" y="76" width="8" height="27" rx="4"/><rect class="${c("quads")}" x="55" y="92" width="12" height="34" rx="5"/><rect class="${c("quads")}" x="73" y="92" width="12" height="34" rx="5"/><rect class="${c("calves")}" x="56" y="126" width="9" height="16" rx="4"/><rect class="${c("calves")}" x="75" y="126" width="9" height="16" rx="4"/><circle class="base" cx="180" cy="25" r="9"/><rect class="${c("shoulders")}" x="160" y="37" width="40" height="12" rx="6"/><rect class="${c("traps")}" x="171" y="47" width="18" height="13" rx="5"/><path class="${c("lats")}" d="M162 52 Q180 60 198 52 L194 83 Q180 91 166 83 Z"/><rect class="${c("triceps")}" x="151" y="48" width="9" height="28" rx="4"/><rect class="${c("triceps")}" x="200" y="48" width="9" height="28" rx="4"/><rect class="${c("glutes")}" x="167" y="84" width="26" height="18" rx="8"/><rect class="${c("hamstrings")}" x="165" y="102" width="12" height="27" rx="5"/><rect class="${c("hamstrings")}" x="183" y="102" width="12" height="27" rx="5"/><rect class="${c("calves")}" x="166" y="129" width="9" height="13" rx="4"/><rect class="${c("calves")}" x="185" y="129" width="9" height="13" rx="4"/></svg>`}
function normMuscle(n){const x=String(n).toLowerCase(),o=[];if(x.includes("chest"))o.push("chest");if(x.includes("shoulder")||x.includes("delt"))o.push("shoulders");if(x.includes("tricep"))o.push("triceps");if(x.includes("bicep")||x.includes("brachialis"))o.push("biceps");if(x.includes("forearm")||x.includes("grip"))o.push("forearms");if(x.includes("lat")||x.includes("mid-back")||x.includes("back extensor"))o.push("lats");if(x.includes("trap"))o.push("traps");if(x.includes("core")||x.includes("oblique"))o.push("core");if(x.includes("glute"))o.push("glutes");if(x.includes("quad")||x.includes("adductor"))o.push("quads");if(x.includes("hamstring"))o.push("hamstrings");if(x.includes("calf")||x.includes("calves")||x.includes("ankle"))o.push("calves");return o}
function renderLibrary(){const q=(el("exerciseSearch").value||"").toLowerCase(),u=[];Object.values(WORKOUTS).forEach(w=>w.ex.forEach(e=>{if(!u.find(x=>x.n===e.n))u.push(e)}));el("exerciseLibrary").innerHTML=u.filter(e=>`${e.n} ${e.primary} ${e.secondary.join(" ")}`.toLowerCase().includes(q)).map(e=>`<article class="panel library-card"><div class="exercise-title">${e.n}</div><div class="exercise-meta">Rest ${e.rest}s</div><div class="muscle-layout"><div class="muscle-map-wrap">${muscleMap([e.primary],e.secondary)}</div><div class="muscle-copy"><div><strong>Primary</strong><span>${e.primary}</span></div><div><strong>Secondary</strong><span>${e.secondary.join(", ")}</span></div><div><strong>Alternative</strong><span>${e.sub}</span></div></div></div><div class="form-guide"><div class="guide-box"><h4>HOW TO DO IT</h4><ul>${e.steps.map(x=>`<li>${x}</li>`).join("")}</ul></div><div class="guide-box"><h4>COMMON MISTAKES</h4><ul>${e.mistakes.map(x=>`<li>${x}</li>`).join("")}</ul></div></div><div class="exercise-links"><a class="link-btn" target="_blank" rel="noopener" href="${e.yt}">YouTube demos ↗</a></div></article>`).join("")}
el("exerciseSearch").oninput=renderLibrary;
function f2url(file){return new Promise((res,rej)=>{if(!file)return res(null);const r=new FileReader();r.onload=()=>res(r.result);r.onerror=()=>rej(r.error);r.readAsDataURL(file)})}function val(id){return el(id).value===""?null:+el(id).value}
async function renderMeasurementDue(){const m=(await all("metrics")).sort((a,b)=>a.createdAt-b.createdAt),last=m.at(-1),box=el("measurementDue");if(!box)return;const days=settings.measurementInterval||14;if(!last){box.innerHTML='<strong>Baseline due now.</strong> Add your first measurements and optional front/side/back photos.';return}const due=new Date(last.date+"T12:00:00");due.setDate(due.getDate()+days);const left=Math.ceil((due-new Date())/86400000);box.innerHTML=left<=0?`<strong>Measurement check-in due.</strong> Compare with your previous ${days===14?"2-week":"4-week"} entry.`:`Next ${days===14?"2-week":"4-week"} check-in: <strong>${due.toLocaleDateString()}</strong> (${left} days).`}
function deltaCard(name,key,unit,a,b){if(a?.[key]==null||b?.[key]==null)return`<div class="compare-box"><small>${name}</small><strong>—</strong></div>`;const d=b[key]-a[key];return`<div class="compare-box"><small>${name}</small><strong>${d>0?"+":""}${d.toFixed(1)} ${unit}</strong></div>`}
function renderMetricComparison(m){const prev=m.at(-2),last=m.at(-1),holder=el("metricComparison");if(holder)holder.innerHTML=prev&&last?[deltaCard("Weight","weight","lb",prev,last),deltaCard("Waist","waist","cm",prev,last),deltaCard("Chest","chest","cm",prev,last),deltaCard("Arm","arm","cm",prev,last),deltaCard("Thigh","thigh","cm",prev,last)].join(""):'<div class="muted">Add at least two measurement check-ins to compare changes.</div>';const pc=el("photoCompare");if(pc){if(!(prev&&last)){pc.innerHTML='<div class="muted">Two photo check-ins are needed for comparison.</div>';return}const cards=[];["front","side","back"].forEach(p=>{if(prev.photos?.[p]&&last.photos?.[p])cards.push(`<div class="photo-entry"><img src="${prev.photos[p]}"><div class="tiny">Previous ${p}</div></div><div class="photo-entry"><img src="${last.photos[p]}"><div class="tiny">Latest ${p}</div></div>`)});pc.innerHTML=cards.join("")||'<div class="muted">No matching photo pairs yet.</div>'}}
el("saveProgressBtn").onclick=async()=>{const rec={id:`metric:${Date.now()}`,date:today(),createdAt:Date.now(),weight:val("metricWeight"),waist:val("metricWaist"),chest:val("metricChest"),arm:val("metricArm"),thigh:val("metricThigh"),note:el("metricNote").value,photos:{front:await f2url(el("photoFront").files[0]),side:await f2url(el("photoSide").files[0]),back:await f2url(el("photoBack").files[0])}};await put("metrics",rec);["metricWeight","metricWaist","metricChest","metricArm","metricThigh","metricNote","photoFront","photoSide","photoBack"].forEach(id=>el(id).value="");toast("Progress check saved");await renderProgress();await renderHome()};
async function renderProgress(){const m=(await all("metrics")).sort((a,b)=>a.createdAt-b.createdAt),s=(await all("sessions")).sort((a,b)=>a.completedAt-b.completedAt);drawChart(el("weightChart"),m.filter(x=>x.weight!=null).map(x=>({y:x.weight})),"Weight");drawChart(el("waistChart"),m.filter(x=>x.waist!=null).map(x=>({y:x.waist})),"Waist");el("metricHistory").innerHTML=m.length?m.slice().reverse().slice(0,12).map(x=>`<div class="history-card"><strong>${x.date}</strong><div class="muted tiny">${x.weight??"—"} lb · waist ${x.waist??"—"} · chest ${x.chest??"—"} · arm ${x.arm??"—"} · thigh ${x.thigh??"—"}</div>${x.note?`<div class="muted tiny">${escapeHtml(x.note)}</div>`:""}</div>`).join(""):'<div class="muted">No body progress entries yet.</div>';renderMetricComparison(m);await renderMeasurementDue()}
function drawChart(c,pts,label){const x=c.getContext("2d"),W=c.width,H=c.height;x.clearRect(0,0,W,H);x.strokeStyle="#e5e9f0";for(let i=1;i<5;i++){const y=i*H/5;x.beginPath();x.moveTo(42,y);x.lineTo(W-14,y);x.stroke()}if(!pts.length){x.fillStyle="#667085";x.font="14px sans-serif";x.fillText("No data yet",20,30);return}const v=pts.map(p=>p.y),min=Math.min(...v),max=Math.max(...v),lo=min===max?min-1:min,hi=min===max?max+1:max,X=i=>42+i*(W-58)/Math.max(1,pts.length-1),Y=n=>H-24-(n-lo)*(H-48)/(hi-lo);x.strokeStyle="#2563eb";x.lineWidth=3;x.beginPath();pts.forEach((p,i)=>i?x.lineTo(X(i),Y(p.y)):x.moveTo(X(i),Y(p.y)));x.stroke();x.fillStyle="#2563eb";pts.forEach((p,i)=>{x.beginPath();x.arc(X(i),Y(p.y),4,0,Math.PI*2);x.fill()});x.fillStyle="#667085";x.font="12px sans-serif";x.fillText(`${label}: ${v.at(-1)}`,42,16)}
el("saveCheckinBtn").onclick=async()=>{await put("checkins",{id:`checkin:${Date.now()}`,date:today(),createdAt:Date.now(),week:settings.currentWeek,weight:val("ciWeight"),sessions:val("ciSessions")||0,sleep:val("ciSleep")||0,energy:val("ciEnergy")||0,pain:el("ciPain").value,good:el("ciGood").value,difficult:el("ciDifficult").value,lifts:el("ciLifts").value});toast("Weekly check-in saved");await renderCoachSummary()};
async function renderCoachSummary(){const a=(await all("checkins")).sort((x,y)=>x.createdAt-y.createdAt);if(!a.length){el("coachSummary").textContent="Complete your first weekly check-in to get coaching guidance.";return}const x=a.at(-1),b=[];if(x.sessions<3)b.push("Adherence was below plan. Prioritize completing all three sessions before adding more volume.");if(x.energy<=2||x.sleep<=2)b.push("Recovery looks low. Reduce volume about 15% next week and prioritize sleep, nutrition, and stress management.");if(x.pain==="Yes")b.push("You reported pain. Substitute the aggravating movement; if pain is sharp, joint-related, or persistent, seek professional evaluation.");if(x.lifts==="Down")b.push("Key lifts trended down. Hold or slightly reduce load and rebuild reps with clean technique.");if(!b.length)b.push("Recovery and adherence look solid. Keep the current structure and increase load only when all valid sets reach the top of the rep range with clean form.");el("coachSummary").textContent=b.join(" ")}
function startRest(sec){if(restTimerId)clearInterval(restTimerId);restLeft=sec;restPaused=false;el("restTimer").classList.remove("hidden");tickRest();restTimerId=setInterval(()=>{if(!restPaused){restLeft--;tickRest();if(restLeft<=0){clearInterval(restTimerId);restTimerId=null;el("restTimer").classList.add("hidden");navigator.vibrate?.([160,80,160]);toast("Rest complete")}}},1000)}function tickRest(){const m=Math.floor(restLeft/60),s=restLeft%60;el("restTimerDisplay").textContent=`${String(m).padStart(2,"0")}:${String(s).padStart(2,"0")}`}el("pauseRestBtn").onclick=()=>{restPaused=!restPaused;el("pauseRestBtn").textContent=restPaused?"Resume":"Pause"};el("skipRestBtn").onclick=()=>{if(restTimerId)clearInterval(restTimerId);restTimerId=null;el("restTimer").classList.add("hidden")};

function showSessionTab(){el("currentSessionTab")?.classList.add("active");el("historyTab")?.classList.remove("active");el("workoutSessionView")?.classList.remove("hidden");el("workoutHistoryView")?.classList.add("hidden")}
async function showHistoryTab(){el("historyTab")?.classList.add("active");el("currentSessionTab")?.classList.remove("active");el("workoutSessionView")?.classList.add("hidden");el("workoutHistoryView")?.classList.remove("hidden");await renderHistory()}
el("currentSessionTab").onclick=showSessionTab;el("historyTab").onclick=showHistoryTab;el("backToSessionBtn").onclick=showSessionTab;
el("startNextWorkout").onclick=async()=>{el("workoutSelect").value=await nextWorkoutId();el("workoutDate").value=today();await navigate("workout")};el("openLastWorkout").onclick=async()=>{await navigate("workout");await showHistoryTab()};
el("exportDataBtn").onclick=async()=>{const data={version:APP_VERSION,settings:await get("settings","main"),sessions:await all("sessions"),drafts:await all("drafts"),metrics:await all("metrics"),checkins:await all("checkins")},b=new Blob([JSON.stringify(data,null,2)],{type:"application/json"}),a=document.createElement("a");a.href=URL.createObjectURL(b);a.download="Farhad-Trainer-V2.3-Backup.json";a.click()};el("resetAppBtn").onclick=async()=>{if(!confirm("Delete all Farhad Trainer V2 data on this device?"))return;for(const s of["settings","drafts","sessions","metrics","checkins"])await clearStore(s);location.reload()};el("clearCacheBtn").onclick=async()=>{if("serviceWorker"in navigator){for(const r of await navigator.serviceWorker.getRegistrations())await r.unregister()}if("caches"in window){for(const k of await caches.keys())await caches.delete(k)}toast("App files refreshed");setTimeout(()=>location.reload(true),500)};
window.addEventListener("beforeinstallprompt",e=>{e.preventDefault();deferredPrompt=e;el("installBtn").classList.remove("hidden")});el("installBtn").onclick=async()=>{if(!deferredPrompt)return;deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;el("installBtn").classList.add("hidden")};

const MEASURE_HELP={
  weight:{title:"Body weight",copy:"Use the same scale on a hard, level surface. Weigh yourself under similar conditions each time—ideally in the morning after using the bathroom and before eating or drinking.",tip:"The app stores body weight in pounds (lb). Day-to-day fluctuations are normal; compare the trend across check-ins.",zone:"scale"},
  waist:{title:"Waist",copy:"Stand relaxed. Wrap a soft tape horizontally around your torso at the level of your navel. Take the reading after a normal exhale. Keep the tape snug, not compressing the skin.",tip:"Use the navel as your landmark every time, even if that is not the narrowest point of your waist.",zone:"waist"},
  chest:{title:"Chest",copy:"Stand relaxed with arms down. Wrap the tape around the fullest part of your chest, roughly nipple level, and keep it horizontal across your back.",tip:"Do not flex or take an unusually deep breath. Measure after a normal breath.",zone:"chest"},
  arm:{title:"Upper arm",copy:"Choose one arm and use that same arm every time. Let the arm hang relaxed and measure around the largest part of the upper arm/biceps.",tip:"Record which side you use (for example, right arm) and keep using that side for future check-ins.",zone:"arm"},
  thigh:{title:"Thigh",copy:"Choose one leg and use that same leg every time. Stand relaxed with weight evenly distributed and measure around the largest part of the upper thigh.",tip:"Pick a repeatable landmark—such as a consistent distance below the hip crease—and use it every time.",zone:"thigh"}
};
function measureIllustration(zone){if(zone==="scale")return `<div class="scale-visual"><div class="scale-feet">◯ &nbsp; ◯</div><div class="scale-box">LB</div></div>`;const y={chest:47,waist:70,arm:58,thigh:105}[zone]||70;const arm=zone==="arm"?`<line x1="37" y1="${y}" x2="51" y2="${y}" class="tape-line"/>`:``;return `<svg viewBox="0 0 100 150" class="measure-body"><circle cx="50" cy="17" r="10"/><path d="M35 34 Q50 26 65 34 L70 86 Q63 101 60 142 L51 142 L49 95 L47 142 L38 142 Q35 104 30 86 Z"/>${arm||`<line x1="27" y1="${y}" x2="73" y2="${y}" class="tape-line"/>`}<circle cx="73" cy="${y}" r="3" class="tape-dot"/></svg>`}
function openMeasureHelp(key){const h=MEASURE_HELP[key];if(!h)return;el("measureModalTitle").textContent=h.title;el("measureModalCopy").textContent=h.copy;el("measureModalTip").textContent=h.tip;el("measureIllustration").innerHTML=measureIllustration(h.zone);el("measureModal").classList.remove("hidden")}
function closeMeasureHelp(){el("measureModal").classList.add("hidden")}
document.querySelectorAll(".measure-help").forEach(b=>b.onclick=()=>openMeasureHelp(b.dataset.measure));el("closeMeasureModal").onclick=closeMeasureHelp;el("measureModal").onclick=e=>{if(e.target===el("measureModal"))closeMeasureHelp()};

(async()=>{await openDB();const s=await get("settings","main");if(s)settings={...settings,...s};else await saveSettings();await migrateWeightsToLbOnce();initControls();renderPlan();renderLibrary();await renderWorkoutPage();await renderProgress();await renderCoachSummary();await renderHome();if("serviceWorker"in navigator)navigator.serviceWorker.register(`./sw.js?v=${APP_VERSION}`).catch(()=>{})})();
